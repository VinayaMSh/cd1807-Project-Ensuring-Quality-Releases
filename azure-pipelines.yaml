name: Azure Pipelines

# Start with a minimal pipeline that you can customize to build and deploy your code.
# Add steps that build, run tests, deploy, and more:
# https://aka.ms/yaml
trigger:
- none

pool: myProject3AgentPool

variables:
  python.version: '3.8.10'
  azureServiceConnectionId: 'project3DemoServiceConn'
  projectRoot: '$(System.DefaultWorkingDirectory)'
  environmentName: 'testEnv'

stages:
- stage: Build
  jobs:
  - job: BuildInfrastructure
    steps:
    # Jmeter tests - Archive the package
    - task: ArchiveFiles@2
      displayName: 'Archive Jmeter TestSuite'
      inputs:
        rootFolderOrFile: $(System.DefaultWorkingDirectory)/automatedtesting/jmeter/performancetests
        includeRootFolder: false
        archiveType: 'zip'
        archiveFile: '$(Build.ArtifactStagingDirectory)/$(Build.BuildId)-perftests.zip'
    # JMeter Test Suite - Publish    
    - publish: $(Build.ArtifactStagingDirectory)/$(Build.BuildId)-perftests.zip
      displayName: 'Upload Package'
      artifact: drop-perftests